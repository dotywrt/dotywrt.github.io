name: Track chasey-dev Repo Updates

on:
  schedule:
    - cron: "0 * * * *" # every hour
  workflow_dispatch:

jobs:
  check-updates:
    runs-on: ubuntu-latest

    env:
      REPO: "chasey-dev/immortalwrt-ipq"
      BRANCH: "24.10-nss-mainline" # change if different

    steps:
      - name: Checkout tracker repo
        uses: actions/checkout@v4

      - name: Get latest commit info
        id: commit
        run: |
          echo "🔍 Checking latest commit for $REPO..."
          DATA=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/$REPO/commits?sha=$BRANCH&per_page=1")

          # Fail if API returns an error
          if echo "$DATA" | jq -e '.[0]' >/dev/null 2>&1; then
            COMMIT_SHA=$(echo "$DATA" | jq -r '.[0].sha')
            AUTHOR=$(echo "$DATA" | jq -r '.[0].commit.author.name')
            DATE=$(echo "$DATA" | jq -r '.[0].commit.author.date')
            echo "commit_sha=$COMMIT_SHA" >> $GITHUB_OUTPUT
            echo "author=$AUTHOR" >> $GITHUB_OUTPUT
            echo "date=$DATE" >> $GITHUB_OUTPUT
          else
            echo "❌ API returned an error: $(echo "$DATA" | jq -r '.message // "Unknown error"')"
            exit 1
          fi

      - name: Stop if commit is not from chasey-dev
        if: steps.commit.outputs.author != 'chasey-dev'
        run: |
          echo "⚠️ Last commit was by '${{ steps.commit.outputs.author }}', not chasey-dev. Skipping."
          exit 0

      - name: Get list of changed files
        run: |
          echo "📂 Fetching changed files for commit ${{ steps.commit.outputs.commit_sha }}"
          FILES=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/$REPO/commits/${{ steps.commit.outputs.commit_sha }}" \
            | jq -r '.files[].filename')
          
          mkdir -p updates
          git clone --depth 1 --branch $BRANCH https://github.com/$REPO.git repo_src
          
          for FILE in $FILES; do
            mkdir -p "updates/$(dirname "$FILE")"
            cp -v "repo_src/$FILE" "updates/$FILE"
          done
          
          echo "✅ Copied updated files into ./updates"

      - name: Upload updated files as artifact
        uses: actions/upload-artifact@v4
        with:
          name: updated-files
          path: updates
